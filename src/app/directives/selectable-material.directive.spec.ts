import { SelectableMaterialDirective } from './selectable-material.directive';

describe('SelectableMaterialDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectableMaterialDirective();
    expect(directive).toBeTruthy();
  });
});
