import { SelectableDirectiveMaterialDirective } from './selectable-directive-material.directive';

describe('SelectableDirectiveMaterialDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectableDirectiveMaterialDirective();
    expect(directive).toBeTruthy();
  });
});
